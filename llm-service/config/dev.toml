app_env = "dev"
business_health_required = true
require_llm_model = false
# AGENT_INTERNAL_SERVICE_TOKEN must be provided by the environment; no token is stored in source.

# Configure any number of selectable models in a private override file or with
# LLM_MODELS as JSON. Never commit real API keys.
# llm_models = [
#   { id = "deepseek", provider = "deepseek", model = "deepseek-chat", apiKey = "replace-me" },
#   { id = "ernie", provider = "qianfan", model = "ernie-4.5-turbo-128k", apiKey = "replace-me" }
# ]
